- お使いのOSによってediting_csv(Windows用).py, editing_csv(macOS用).pyのどちらかを選択したください。

- Windows用の場合は、Cドライブ下にcsvディレクトリを作成後csvディレクトリ下にsample.csvを作成してください。

例：
```
C:/csv/sample.csv
```

- macOS用の場合は、/Users/yourname下にcsvディレクトリを作成後csvディレクトリ下にsample.csvを作成してください。

例：
```
/Users/yourname/csv/sample.csv
```